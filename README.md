# ClientFlow-CRM
ClientFlow CRM is a full-stack client management system built with React, Node.js, and PostgreSQL. It supports JWT auth, role-based access (admin/user), client &amp; task management, file uploads, search, and dashboard analytics. Built as a SaaS-style portfolio project.
